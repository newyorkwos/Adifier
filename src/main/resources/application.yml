spring:
  profiles:
    active: dev
  devtools:
    restart:
      trigger-file: .trigger
  datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://127.0.0.1:3306/wuschema?useUnicode=true&characterEncoding=utf-8
      username: wumy
      password: pass1234
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate.format_sql: true

